# 가져온 모듈

`doc-tools.js`, `doc-rules.js`, `collab-ops.js` 는 형제 프로젝트 `Desktop/code/hwpx에디터기반AI/public/` (커밋 866416b, 2026-09-25) 에서 **고치지 않고** 복사했다.
같은 rhwp 0.8.6 `HwpDocument` 위에서 도는 순수 모듈이다(26종 편집 도구와 스냅샷 보호, 문서 개요, 행정문서 검수, 글 모델).
갱신할 때는 그쪽 파일을 다시 복사하고 `node setup.mjs` 로 재빌드한다. 여기서 직접 고치지 않는다.
